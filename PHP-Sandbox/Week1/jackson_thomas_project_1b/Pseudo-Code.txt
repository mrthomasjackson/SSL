Pseudo-code (Form Processing)HTML FORM- Enter first name, last name, username, password, and avatar image into HTML form.- form validates using HTML validation- IF form does not validateo Display error message with where the user missed filling in information- ELSEo Run newUser.phpnewUser.php- Store form data into an associative array “newUser”o firstname[0] => _POST[‘firstname’]o lastname[1] => _POST[‘lastname’]o username[2] => _POST[‘username’]o password[3] => encryptPasswordOnEntry(_POST[‘password))* PASSWORD WILL BE ENCRYTPED BEFORE BEING SAVED INTO ASSOCIATIVE ARRAYo file[4] => $_FILES[‘file’]- Store image o Set target directory to “uploads/”o Set the path of the target file* “target directory” + “/” + basename(newUser['file’]) + “name”o IF file exists, * RETURN TRUEo Else* RETURN TRUE- Print form items to the browsero FOR EACH item in the associative array EXCEPT AVATAR IMAGE* Echo ITEM- Display the avatar image