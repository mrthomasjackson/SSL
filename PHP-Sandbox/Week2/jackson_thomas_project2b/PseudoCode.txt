Pseudo-Code (Fruits Application)HTML FORM1. User visits fruits.php2. User enters dataa. Fruit nameb. Fruit Color3. User submits form4. HTML validates forma. IF HTML validates Ð Send to PHP scriptb. ELSE Ð Return error messagePHP Processing 1. Connect to MySQL database (PDO)2. IF there is a POSTa. Enter data into database3. Else (NO POST)a. Break action4. Loop through each row in database and return as HTML for the user to see Ð Give option to delete fruit itema. fruitIDb. fruitNamec. fruitColor5. IF user clicks deletea. Direct to deletefruit.phpb. Connect to databasec. GET fruitIDd. Run SQL Statement to delete fruit by fruitIDe. Redirect back to fruits.php